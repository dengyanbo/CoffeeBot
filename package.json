{
  "name": "coffee-bot",
  "version": "1.0.0",
  "description": "Teams coffee ordering bot with quota management",
  "main": "dist/CoffeeFunc.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start --typescript | cat",
    "dev": "func start --typescript | cat",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@azure/functions": "^4.5.1",
    "botbuilder": "^4.21.1",
    "@azure/data-tables": "^13.2.2",
    "dayjs": "^1.11.10"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "azure-functions-core-tools": "^4.0.5455",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
